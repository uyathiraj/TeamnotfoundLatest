<Page
    x:Class="teamnotfound.View.MyPost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:teamnotfound.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:teamnotfound.DataModel"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock Text="PROJECTS" HorizontalAlignment="center" FontSize="30" Margin="50" FontWeight="Bold"></TextBlock>
        <ListView Name="gridView" Margin="63,97,63,50"  ItemClick="gridView_ItemClick" >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Project">
                    <StackPanel HorizontalAlignment="Center" Background="DimGray" Tapped="StackPanel_Tapped">
                        <TextBlock FontSize="16" Text="{x:Bind Description}"></TextBlock>
                        <TextBlock FontSize="10" Text="{x:Bind Type}"></TextBlock>
                        <TextBlock FontSize="10" Text="{x:Bind Bid}"></TextBlock>
                        <TextBlock FontSize="10" Text="{x:Bind Owner}"></TextBlock>
                        <TextBlock FontSize="10" Text="{x:Bind Status}"></TextBlock>

                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
